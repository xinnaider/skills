# skills

Skills pessoais para CLIs de IA (Claude / claude-pessoal).

- `model-routing/` — roteamento de modelos e orquestração por subagents.
- `criar-pr/` — criação de pull request.

Uso: symlink de cada subpasta em `~/.claude/skills/`.
